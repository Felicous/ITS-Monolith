# Setup
To setup the project `npm insatll` needs to be executed.

# Start
1. Execute `npm run filehost` in the terminal.
2. Execute `npm run dev` in another instance of the terminal.

# AI
If you want to use the ai you need to paste a Gemini Key into the File `.env.local` behind the `=`.